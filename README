# Usage

git clone git://github.com/cyx/lua-setup.git
cd lua-setup; source install.sh

# Prerequisites

    sudo apt-get install build-essential curl libreadline6-dev \
            libcurl4-openssl-dev uuid-dev

# What gets installed?

1. lua
2. lsocket
3. cmsgpack
4. pac
5. md5 / sha1 / etc
6. base64
7. uuid
